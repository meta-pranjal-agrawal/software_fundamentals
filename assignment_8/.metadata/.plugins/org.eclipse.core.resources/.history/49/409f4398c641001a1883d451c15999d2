import java.util.ArrayList;
import java.util.List;


public class Zoo 
{
	
	List<Zone> zoneList= new ArrayList<Zone>();
	
	public void addZone(String category,int limitOfCages,boolean hasPark,boolean hasCanteen)
	{
		Zone z1= new Zone(category,limitOfCages,hasPark,hasCanteen);
		zoneList.add(z1);
		
	}
	
	public boolean addCage(String category,int capacity,String type)
	{
		for(int i=0;i<zoneList.size();i++)
		{
			if(zoneList.get(i).category==category && zoneList.get(i).limitOfCages>zoneList.get(i).numberOfCages)
			{
				zoneList.get(i).zone.add(new Cage(type,capacity));
				zoneList.get(i).numberOfCages++;
				return true;
			}
		}
		
		return false;
	}
	
	public boolean addAnimal(String category,String type,String name,int age,float weight)
	{
		for(int i=0; i<zoneList.size(); i++)
		{
			if(zoneList.get(i).equals("Mammal"))
			{
				
				if(zoneList.get(i).zone.get(i))
				
			}
		}
		
		for(int i=0; i<zoneList.get(i).numberOfCages; i++)
		{
			if(zoneList.get(i).numberOfCages<zoneList.get(i).limitOfCages)
			{
				zoneList.get(i).zone.get(i).cage.add()
			}
		}
		
		
		return false;
	}
	
	


}
